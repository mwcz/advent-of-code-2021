{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "codelldb",
            "request": "launch",
            "name": "Debug executable 'binary'",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/target/debug/part1bfs",
            "args": [
                "input-small"
            ]
        }
    ]
}
